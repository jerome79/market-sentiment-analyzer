.PHONY: format lint test precommit coverage

format:
	black .
	ruff check . --fix

lint:
	ruff check .

test:
	pytest -q

coverage:
	pytest -q --cov=market_sentiment_analyzer --cov-report=term-missing

precommit:
	pre-commit run --all-files
